plugins {
    id("pl.allegro.tech.build.axion-release") version "1.18.16"
}

scmVersion {
    versionCreator('simple')
    //releaseOnlyOnReleaseBranches = trueadsf
    //releaseBranchNames = ['main']asdf
}

version = scmVersion.version